# Deliberately minimal, in the spirit of the R-ASBS repository: no Hydra,
# no Lightning, no experiment framework.  Everything runs from plain scripts.
#
# These are the exact versions every number in README.md was produced with
# (conda env SML_env, Python 3.11, torch built against CUDA 12.4, 2x A100 80GB).
# Nearby versions almost certainly work; the pins are here so that a reader who
# gets a different number knows the environment is not the reason.
torch==2.5.1
numpy==2.4.6
scipy==1.17.1
matplotlib==3.11.1

# Only needed for the GB1 fixed-support experiment (Appendix A.2): reads the
# two eLife 2016;5:e16965 supplementary .xlsx workbooks.  Not imported by any
# other experiment.
openpyxl==3.1.5
